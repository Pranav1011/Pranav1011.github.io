---
title: F1 Race Intelligence Agent
headline: Ask seven seasons of Formula 1 anything
domain: Formula 1 analytics
slug: f1-ria
order: 2
featured: true
year: 2026
role: Solo — design, build, measurement
summary: An AI assistant that answers plain-English questions about seven seasons of Formula 1, with answers built from the race data it retrieves.
problem: The assistant had grown to 79 narrow, overlapping tools. That’s a lot for an AI to choose from, and a lot of definition text to carry around.
mechanisms:
  - label: Plans each question
    detail: "An AI planner decides what data to pull: lap timing, a knowledge graph, or race reports."
  - label: Answers from data
    detail: Replies are built from the race data it retrieves.
  - label: Fewer, clearer tools
    detail: I regrouped the 79 tools into 8 without removing anything, and a test proves it.
result: The tool definitions are 85% smaller, from 13,487 to 2,014 tokens, and all 79 original capabilities still work.
metric:
  value: "−85%"
  label: smaller tool definitions
stack: [LangGraph, FastAPI, TimescaleDB, Neo4j, Qdrant, Next.js]
links:
  repo: https://github.com/Pranav1011/F1-Race-Intelligence-Agent
cover:
  src: /media/f1ria-loop.mp4
  poster: /media/f1ria-loop-poster.jpg
  alt: Animation of 79 tiles, one for each of the agent's original tools, collapsing into the 8 tools they now sit under, labelled race_analysis, driver_stat, standings, compare, whatif, get_available_sessions, get_database_schema and query_f1_sql.
  kind: video
status: active
---

import F1Architecture from '../../components/figures/F1Architecture.astro';
import F1Results from '../../components/figures/F1Results.astro';
import OverviewVideo from '../../components/OverviewVideo.astro';

{/* src (frontmatter): backend/eval/results/consolidation.json; backend/tests/test_tools/test_consolidated.py */}

<OverviewVideo
  src="/media/f1ria-overview.mp4"
  poster="/media/f1ria-overview-poster.jpg"
  width={1280}
  height={720}
  lines={[
    'Ask seven seasons of Formula 1 anything: 2018–2024 race data.',
    'A real question: “Compare Verstappen and Norris’s tire degradation in the 2024 Singapore GP.” It picks the right tool for it: race_analysis, then get_tire_degradation.',
    'And answers from the race data: real lap times, 2024 Singapore GP, hard tyres, laps 31–62.',
    '79 tools: it had grown to one for every kind of question. Grouped into eight, and every one of the 79 still works.',
    '85% smaller tool definitions (13,487 to 2,014 tokens). Nothing lost.',
  ]}
  credit="Music: Sascha Ende (ende.app), CC BY 4.0."
/>

{/* src (video): brag run on the F1 RIA repo; ledger in its local brag output (README question, consolidated_tools.py, tests/test_tools/test_consolidated.py, eval/results/consolidation.json; the chart from FastF1 data for that race) */}

## TL;DR

- **Problem:** a Formula 1 question-answering agent had 79 overlapping tools.
- **What I did:** regrouped them into 8 clear tools, with a test proving nothing was lost.
- **Result:** tool definitions 85% smaller (13,487 → 2,014 tokens), all 79 capabilities intact.

## The problem

The agent answers questions like "Compare Verstappen and Norris's tyre degradation in Singapore 2024" or "What if Max had pitted on lap 33 instead of 38?" It covers seven seasons, 2018 to 2024. To do that it sits on a polyglot store: TimescaleDB for lap and telemetry time series, Neo4j for the knowledge graph of drivers, teams and races, and a Qdrant vector index for retrieval.

{/* src: F1 RIA/README.md Overview ("7 years of data (2018-2024)"), Architecture */}

As features grew, so did the tool list. By the time I measured it, the agent had 79 TimescaleDB tools whose definitions came to 13,487 tokens. That’s a lot of surface for a model to choose from: the model has to pick one function out of 79 near-duplicates like "lap times by driver", "lap times by stint" and "lap times by compound."

{/* src: CLAIMS.md (public in F1 RIA) scope note (79 TimescaleDB tools = 88% of original token tax) */}

<F1Architecture n={1} />

## Approach

**Measure first.** Before changing anything, I wrote a measurement script that serializes the agent's tool definitions the way LangChain formats them for a model and counts tokens with tiktoken. That gave a baseline of 13,487 tokens for the TimescaleDB tools alone.

{/* src: backend/eval/measure_consolidation.py; consolidation.json */}

**Consolidate by intent, not by table.** The 79 tools collapsed naturally into a few questions people actually ask: race analysis, driver stats, standings, comparisons and what-if scenarios. I built eight parameterized dispatchers on those lines. Each one routes to the original underlying function and filters arguments to that function's signature, so none of the originals had to change.

{/* src: backend/agent/tools/consolidated_tools.py; test_dispatch_routes_and_filters_args */}

**A guarded escape hatch.** For questions the dispatchers don't cover, the agent falls back to text-to-SQL that is validated as read-only before it runs. The model gets flexibility without write access to the database.

{/* src: backend/agent/validation.py; tests/test_tools/test_sql_validation.py */}

**Grounding.** A query-understanding step parses each question into entities, sub-questions and a HyDE-style hypothetical answer describing what a complete response should contain. The plan is built from that, and answers are built from the race data and telemetry it retrieves.

{/* src: HyDE — agent/prompts/understand.py:21, agent/nodes/understand.py:139 (main). NOTE: hypothetical_answer is generated but not yet passed to vector search; see docs/SOURCES.md */}

**The rest of the system.** The agent runs on LangGraph behind FastAPI, with a multi-provider LLM router (Groq's Llama 3.3 70B, falling back to Gemini, then a local Ollama model). It serves sub-second responses on million-row datasets, and a Next.js front end adapts its layout to the question: speed traces for telemetry, timelines for strategy, side-by-side views for comparisons.

{/* src: README Tech Stack, Features (private: SOURCES.md for latency) */}

## Results

| Measure | Before | After |
|---|---|---|
| Tools | 79 | 8 |
| Tool-definition size (tokens) | 13,487 | 2,014 |
| Underlying capabilities reachable | 79 | 79 |
| Test suite | 57 passing | 63 passing |

{/* src: consolidation.json; CLAIMS.md (public in F1 RIA) rows 1–3, 6 */}

<F1Results n={2} />

The zero-loss claim is enforced by a test, not asserted: `test_every_timescale_tool_is_covered_exactly_once` fails if any of the 79 original functions becomes unreachable or is covered twice. The token numbers come from the same measurement script before and after, so anyone can reproduce them with one command.

{/* src: CLAIMS.md (public in F1 RIA) row 3 */}

## What's next

The consolidation covers the TimescaleDB tools, which were 88% of the original token cost. The 13 Neo4j and vector tools are next. The bigger open question is accuracy: fewer, better-named tools should improve tool selection, and I want to prove that with a labelled question set scored end to end, rather than assume it.

{/* src: CLAIMS.md (public in F1 RIA) scope note */}
